% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radii_management.R
\name{set_radii_data}
\alias{set_radii_data}
\title{Set or Reset a Custom Atomic Radii Table}
\usage{
set_radii_data(radii_data = NULL)
}
\arguments{
\item{radii_data}{A \code{data.table} or \code{data.frame} containing the custom radii
data, or \code{NULL} to reset to the package default.}
}
\value{
Invisibly returns \code{NULL}. A message is printed to the console
confirming the action.
}
\description{
Allows the user to provide their own table of atomic radii for
the current R session. This custom table will be used by functions like
\code{filter_ghost_distances}.
}
\details{
The provided \code{data.table} or \code{data.frame} must contain at least three columns:
\itemize{
\item \code{Symbol} (character): The chemical symbol of the element (e.g., "Si").
\item \code{Radius} (numeric): The atomic radius in Angstroms (Å).
\item \code{Type} (character): A descriptor for the radius type (e.g., "covalent", "ionic").
}
This allows for storing multiple types of radii in the same table, which can
be selected using the \code{radii_type} argument in relevant functions.

To revert to using the package's default radii table, call the function
with \code{NULL} or without any arguments.
}
\examples{
# 1. Create a custom radii table with both covalent and ionic radii
my_radii <- data.table::data.table(
  Symbol = c("Si", "O", "O"),
  Radius = c(1.11, 0.66, 1.40),
  Type   = c("covalent", "covalent", "ionic")
)

# 2. Set this table for the current session
set_radii_data(my_radii)

# Now, any subsequent calls to filter_ghost_distances() will use this table.
# You could, for example, filter based on ionic radii by setting radii_type = "ionic".

# 3. Reset to the package's default covalent radii table
set_radii_data(NULL)

}
\seealso{
Other post-processing: 
\code{\link{calculate_weighted_average_network_distance}()},
\code{\link{export_analysis_to_csv}()},
\code{\link{filter_ghost_distances}()}
}
\concept{post-processing}
